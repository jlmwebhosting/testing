const assert = require( "should" );

//: @server:
const <module> = require( "<module>" );
//: @end-server


//: @server:

describe( "<module>", ( ) => {

	describe( <test>, ( ) => {

		it( <message>, ( ) => {

		} );

	} );

} );

//: @end-server
